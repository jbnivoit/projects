struct GetInfoElement { char *name; unsigned int val; };
%%
title, PARSING_TITLE,
description, PARSING_DESCRIPTION,
comments, PARSING_COMMENTS,
note, PARSING_NOTE,
tag, PARSING_TAG,
url, PARSING_URL,
rsp, PARSING_UNKNOWN,
photo,  PARSING_PHOTO,
owner,  PARSING_OWNER,
visibility,  PARSING_VISIBILITY,
dates,  PARSING_DATES,
permissions,  PARSING_PERMISSIONS,
editability,  PARSING_EDITABILITY
